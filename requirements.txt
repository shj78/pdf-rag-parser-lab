# Base editable install with development tooling and parser dependencies.
-e .[dev,parsers]

# When the scaffold turns into runnable experiments, this can be expanded to:
# -e .[dev,parsers,retrieval,evaluation]
